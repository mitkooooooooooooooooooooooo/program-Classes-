namespace _03._Elevator
{

  class Program
  {
        static void Main()
        {
            int n = int.Parse(Console.ReadLine());
            int p = int.Parse(Console.ReadLine());

            int coursesNeeded = (int)Math.Ceiling((double)n / p);

            Console.WriteLine(coursesNeeded);
        
        
        
        
        
        
        
        }
  }
   
}